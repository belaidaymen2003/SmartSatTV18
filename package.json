{
  "name": "streaming-app",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "admin",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=admin",
    "build": "npm run build --workspace=admin && npm run build --workspace=client"
  }
}
